/**
 * App Theme - Sizes
 *
 * */
import {Dimensions} from 'react-native';

const {width, height} = Dimensions.get('window');

export default {
    // Window Dimensions
    screenHeight: height,
    screenWidth: width,
};
